// Display string in an alert box

alert(`You have received this message because you have been chosen to open an important value. Here is the secret combination:`)

// Assign three variables - each using different mathmatical equations

const firstNumber = 110 % 100 //should equal 10
const secondNumber = 25 + 15 //should equal 40
const thirdNumber = secondNumber - 1 //should equal 39

// Create code to display combination in an alert box

alert(`The code is: ${firstNumber} - ${secondNumber} - ${thirdNumber}`)